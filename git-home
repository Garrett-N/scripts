# handle git config
cp $HOME/.gitconfig-garrett-n $HOME/.gitconfig

# ssh agent to handle the ssh keys
ssh-add -D
ssh-add ~/.ssh/46_github

# good to go
echo remember to use: 
echo     git clone git@github.com: blah...
